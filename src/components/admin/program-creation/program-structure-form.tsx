'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export function ProgramStructureForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Program Structure</CardTitle>
        <CardDescription>
          Configure the workout structure and program flow
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Program structure form coming soon...</p>
      </CardContent>
    </Card>
  );
}