/**
 * Global setup for integration tests
 * Runs once before all tests
 */
export default async function globalSetup() {
  // Note: NODE_ENV is set via Jest configuration
  console.log('🔧 Global test setup complete');
}
